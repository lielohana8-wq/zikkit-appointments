# CLAUDE.md — תדריך פרויקט זיקית (Zikkit)
> קובץ ידע לכל סשן Claude (Code / Design). קרא במלואו לפני כל שינוי.

## 1. מה זה זיקית
SaaS ישראלי שהופך עסקי תורים (מספרות, ציפורניים, קוסמטיקה, קליניקות) לעסק אוטומטי:
- **דף הזמנות = "האפליקציה של העסק"** — כל עסק מקבל PWA ממותגת (לוגו, צבעים, ערכת עיצוב) שהלקוחות שומרים למסך הבית וקובעים בה תורים לבד.
- **דשבורד ניהול** לבעל העסק והצוות: יומן חי, לקוחות, דוחות, שכר.
- **דנה** — מזכירת AI קולית (בפיתוח) שעונה לשיחות וקובעת תורים.
- מודל עסקי: ₪199/חודש, מייסדים ₪149. שני עסקי פיילוט חיים + עסק דמו BARBER CLUB.

## 2. סטאק וארכיטקטורה
- **Next.js 14.2.5 App Router + TypeScript + MUI v5**, RTL מלא, עברית.
- **Firebase**: Firestore (מסמך אחד לעסק! doc id = uid של הבעלים), Storage לתמונות, Auth.
- API routes תחת `src/app/api/*` — כולל public-booking, manage-booking, push, sms (Twilio), reminders/cron (שעתי), hq/*, platform/stats, track (טלמטריה), pilot-request (לידים).
- מסמכי מערכת בקולקציה: `arch_*` (ארכיון), `bak_*` (גיבוי), `_platform` — מסוננים ב-listAllBiz. לא לגעת.
- deploy: Vercel, אוטומטי מ-push ל-main בריפו `lielohana8-wq/zikkit-appointments`.

## 3. שפת העיצוב (זהות המותג — לשמר!)
- **Dark-luxury**: רקע `#0A0710`, גרדיאנט מותג `#7C3AED → #EC4899`, זהב-סגלגל `#C9A0FF` להדגשות.
- טיפוגרפיה: **Heebo** לטקסט, **Playfair Display** ללוגו/כותרות יוקרה (letter-spacing רחב).
- טוקנים: `src/styles/theme.ts` → `zikkitColors` (c.bg, c.surface1-3, c.border/2, c.text/2/3, c.accent, c.hot, c.green). **כל צבע חדש — דרך הטוקנים בלבד.**
- דף ההזמנות תומך **ערכות פר-עסק** (כהה/בהיר/רך/נועז) דרך אובייקט `th` + branding מה-API. חובה לשמר את מנגנון הערכות — אין לקבע צבעים בדף הזמנות.
- שפה עיצובית: פינות עגולות נדיבות (borderRadius 3-4 / 99 לצ'יפים), משטחים שקופים-עדינים על כהה, אימוג'י כאייקונים פונקציונליים (📅 ✂️ 🔔), צל עמוק לכרטיסים מרכזיים.

## 4. מוקשים (נלמדו בדם — אל תחזור עליהם)
1. **עברית ב-JSX**: מרכאות בעברית (`"סה"כ"`) שוברות SWC — השתמש ב-`&quot;` או גרש בודד. אימוג'י תקין.
2. **תנאי חד-שורש**: `{cond && (<Box/>)}` — הוספת אח שני בתוך הסוגריים שוברת קומפילציה ("Unexpected token, expected jsx identifier"). הוסף מחוץ ל-`)}` או עטוף בפרגמנט.
3. **מסמך עסק = 1MB מקס**: אסור להוסיף שדות כבדים/מערכים גדלים ללא ארכוב. תמונות → Storage בלבד (uploadImageToStorage), לעולם לא base64 במסמך.
4. **book page (`src/app/book/[bizId]/page.tsx`) הוא ענק (1,200+ שורות)** — ערוך בו נקודתית; אל תעשה reformat גלובלי.
5. יש `track` ותיק (analytics ישן) בדף ההזמנות — הביקון החדש נקרא `beacon`. אל תתנגש.
6. שינוי שם איש-צוות חייב לעבור דרך המפל (cascade) הקיים — תורים משויכים לפי שם.
7. אחרי כל שינוי: `npm run build` חייב לעבור נקי (0 שגיאות). אין commit על build אדום.

## 5. מה מותר ומה אסור (לסשן עיצוב)
**מותר ורצוי:**
- דשבורד 2.0: היררכיה ("היום שלך" למעלה), פחות עומס כרטיסים, גרף מגמה שבועי.
- מיקרו-אנימציות (מעברי מסך, hover, ריפוד סקלטון בטעינות).
- ריווח/טיפוגרפיה עקביים, האחדת גדלים, מצבי ריק (empty states) יפים.
- שיפור נראות מסכי ניהול (לקוחות/דוחות/הגדרות) — הם תפוצתיים פחות מדף ההזמנות.
**אסור בלי אישור:**
- לשבור את מנגנון הערכות פר-עסק בדף ההזמנות, או לשנות את זרימת ההזמנה (service→staff→slot→details→done).
- ספריות UI חדשות / החלפת MUI / CSS frameworks.
- שינוי סכמת Firestore או API contracts.
- מחיקת עמודים — יש עמודי לגאסי (courses, stations, expenses…) שמיועדים למחיקה, אבל זה ספרינט נפרד ומכוון.

## 6. מסכים מרכזיים (סדר חשיבות)
1. `/book/[bizId]` — דף ההזמנות (הפנים של המוצר; זהירות כפולה)
2. `/dashboard` — מרכז הבעלים · 3. `/calendar` — יומן חי · 4. `/booking-page` — סטודיו העיצוב
5. `/team`, `/customers`, `/staff-report`, `/revenue`, `/reviews`, `/waitlist`, `/settings`
6. `/welcome` — אשף אונבורדינג · `/hq` — פאנל-על של ליאל (מוגן אימייל) · `/login`
7. `/assistant` — צ'אט Ziki · `/terms`, `/accessibility`

## 7. תהליך עבודה
- עבוד על branch או ישירות על main בקומיטים קטנים וממוקדים; הודעות קומיט באנגלית קצרה.
- כל שינוי: build ירוק → commit → push (Vercel יעלה לבד).
- ספק בכל שאלה מוצרית: עצור ושאל את ליאל. עברית, תמציתי.
